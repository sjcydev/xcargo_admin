// import { toast } from '@zerodevx/svelte-toast';

// export async function validateUser(usuario: Registro): Promise<boolean> {
// 	let valid = true;

// 	if (usuario.contraseña !== usuario.confirmar) {
// 		toast.push('Contraseñas no son iguales', { classes: ['warning'] });
// 		valid = false;
// 	}

// 	return valid;
// }
